# 电力系统恢复网架重构优化模型

## 1. 问题背景

电力系统大停电后，从黑启动母线开始，通过逐步闭合前沿线路恢复网架和负荷。本模型为多时间步混合整数线性规划（MILP，属于 MIP），约束拓扑恢复与系统总功率供需，不含线路潮流约束；目标为最大化累计恢复负荷效益并惩罚负荷缺额。

$t=0$ 为初始状态；初始仅黑启动母线通电、线路全部断开、负荷全部为 0 挡。每个动作时步闭合一条前沿线路，负荷挡位同步决策；新通电母线的负荷最早在下一状态投入，直至 $H=38$ 个动作完成。

---

## 2. 集合、变量与参数

### 2.1 集合

| 符号 | 含义                      |
|------|---------------------------|
| $B$ | 母线集合|
| $L$ | 线路集合|
| $G$ | 发电机集合 |
| $G_{\mathrm{conv}}$ | 常规发电机集合|
| $G_{\mathrm{WT}}$ | 风电机组集合|
| $D$ | 负荷集合 |
| $\mathcal T_s=\{0,1,\ldots,H\}$ | 状态时步集合|
| $\mathcal T_a=\{0,1,\ldots,H-1\}$ | 动作时步集合|
| $\delta_L(b)=\{l_{ij}\in L:b=i\ \text{或}\ b=j\}$ | 与母线 $b$ 关联的线路集合 |

其中，$H=|B|-1=38$，网络 $(B,L)$ 连通，$G=G_{\mathrm{conv}}\cup G_{\mathrm{WT}}$，$G_{\mathrm{conv}}\cap G_{\mathrm{WT}}=\varnothing$，且 $g_{\mathrm{BS}}\in G_{\mathrm{conv}}$。

### 2.2 索引与映射

| 符号 | 含义                                   |
|------|----------------------------------------|
| $g_{\mathrm{BS}}$ | 黑启动机组                             |
| $b_{\mathrm{BS}}=b(g_{\mathrm{BS}})$ | 黑启动机组所在母线，本项目为 30 号母线 |
| $l_{ij}$ | 连接母线 $i$ 与 $j$ 的线路，$i,j\in B$，$i<j$ |
| $w$ | 风电机组索引，$w\in G_{\mathrm{WT}}$ |
| $b(g)$ | 发电机 $g$ 所在母线                    |
| $b(d)$ | 负荷 $d$ 所在母线                      |

### 2.3 参数

| 符号 | 单位 | 含义                                   |
|------|------|----------------------------------------|
| $\Delta t$ | h | 时间步长，$\Delta t=1/4$               |
| $P_g^{\max}$ | MW | 发电机 $g$ 的最大出力                  |
| $R_g$ | MW/步 | 发电机 $g$ 的最大爬坡量                |
| $P_d^{\max}$ | MW | 负荷 $d$ 的额定功率                    |
| $\overline P_{w,t}^{\mathrm{av}}$ | MW | 风电机组 $w$ 在状态时步 $t$ 的可用出力 |
| $\lambda_{\mathrm{shed}}$ | — | 负荷缺额惩罚权重，取 $5$               |

---

## 3. 决策变量

| 变量 | 类型 | 含义                                            |
|------|------|-------------------------------------------------|
| $u_{b,t}$ | $\in\{0,1\}$ | 状态 $t$ 时母线 $b$ 是否通电                    |
| $y_{l_{ij},t}$ | $\in\{0,1\}$ | 动作时步 $t$ 是否闭合线路 $l_{ij}$              |
| $k_{d,t}$ | $\in\{0,1,2\}$ | 状态 $t$ 时负荷 $d$ 的恢复挡位,分别表示负荷投入 0%、50% 和 100%                 |
| $p_{g,t}$ | $\geq 0$ | 状态 $t$ 时发电机 $g$ 的有功出力（MW）          |
| $s_t$ | $\geq 0$ | 状态 $t$ 时系统负荷超过机组实际出力的缺额（MW） |

---

## 4. 约束条件

### 4.1 初始状态约束

**C0a — 初始时仅黑启动母线通电：**
$$
u_{b,0}=\begin{cases}1,&b=b_{\mathrm{BS}},\\0,&b\ne b_{\mathrm{BS}},\end{cases}
\qquad \forall b\in B
$$

**C0b — 初始时所有机组出力为零：**
$$
p_{g,0}=0,
\qquad \forall g\in G
$$

**C0c — 初始时所有负荷均未投入：**
$$
k_{d,0}=0,\qquad \forall d\in D
$$

### 4.2 线路动作约束

**C1 — 每个动作时步恰好闭合一条线路：**
$$
\sum_{l_{ij}\in L}y_{l_{ij},t}=1,
\qquad \forall t\in\mathcal T_a
$$

**C2a — 所选线路不能连接两个未通电母线：**
$$
y_{l_{ij},t}\le u_{i,t}+u_{j,t},
\qquad \forall l_{ij}\in L,\ \forall t\in\mathcal T_a
$$

**C2b — 所选线路不允许形成恢复环路：**
$$
y_{l_{ij},t}\le 2-u_{i,t}-u_{j,t},
\qquad \forall l_{ij}\in L,\ \forall t\in\mathcal T_a
$$

### 4.3 母线通电约束

**C3a — 母线通电状态不可回退：**
$$
u_{b,t+1}\ge u_{b,t},
\qquad \forall b\in B,\ \forall t\in\mathcal T_a
$$

**C3b — 闭合线路的两端母线在下一状态通电：**
$$
u_{i,t+1}\ge y_{l_{ij},t},\qquad
u_{j,t+1}\ge y_{l_{ij},t},
\qquad \forall l_{ij}\in L,\ \forall t\in\mathcal T_a
$$

### 4.4 负荷恢复约束

**C4a — 负荷恢复挡位不可回退：**
$$
k_{d,t+1}\ge k_{d,t},
\qquad \forall d\in D,\ \forall t\in\mathcal T_a
$$

**C4b — 负荷所在母线须在前一状态已通电，负荷方可投入：**
$$
k_{d,t+1}\le 2u_{b(d),t},
\qquad \forall d\in D,\ \forall t\in\mathcal T_a
$$

**C4c — 负荷挡位决定系统总负荷：**
$$
P_L(t)=\sum_{d\in D}\frac{P_d^{\max}}{2}k_{d,t},
\qquad t\in\mathcal T_s
$$

### 4.5 发电机出力与供需约束

**C5a — 机组出力不超过容量上限，且新通电母线上的机组延迟一个状态出力：**
$$
\begin{aligned}
p_{g,t}&\le P_g^{\max}u_{b(g),t-1}, &&\forall g\in G,\quad t=1,\ldots,H,\\
p_{w,t}&\le \overline P_{w,t}^{\mathrm{av}}u_{b(w),t-1}, &&\forall w\in G_{\mathrm{WT}},\quad t=1,\ldots,H
\end{aligned}
$$

**C5b — 机组向上爬坡量不超过最大爬坡量：**
$$
p_{g,t}-p_{g,t-1}\le R_g,
\qquad \forall g\in G,\quad t=1,\ldots,H
$$

**C5c — 系统负荷缺额：**
$$
s_t=\max\left\{0,\,P_L(t)-\sum_{g\in G}p_{g,t}\right\},
\qquad t=1,\ldots,H
$$

---

## 5. 目标函数

最大化累计恢复负荷效益并扣除负荷缺额惩罚：
$$
\max\quad J=0.25sum_{t=1}^{H}\left(P_L(t)-\lambda_{\mathrm{shed}}s_t\right)
=0.25um_{t=1}^{H}\left(\sum_{d\in D}\frac{P_d^{\max}}{2}k_{d,t}
-\lambda_{\mathrm{shed}}s_t\right).
$$
